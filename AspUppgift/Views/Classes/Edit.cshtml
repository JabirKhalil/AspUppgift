@model List<AddTeacher>

@{ var classId = ViewBag.classId; }
<div class="row">
    <div class="col-6">
        <form method="post">
            @for (int i = 0; i < Model.Count; i++)
            {
    <div class="form-check m-1">

        <input type="hidden" asp-for="@Model[i].UserId" />
        <input type="hidden" asp-for="@Model[i].UserName" />
        <input asp-for="@Model[i].IsSelected" class="form-check-input" />
        <label class="form-check-label" asp-for="@Model[i].IsSelected">
            @Model[i].UserName
        </label>
    </div>}
            <input type="submit" value="Lägg till lärare" class="btn btn-primary" />


        </form>
    </div>
    <div class="col-6">
        <form method="post">
            @for (int i = 0; i < Model.Count; i++)
            {
    <div class="form-check m-1 ">

        <input type="hidden" asp-for="@Model[i].UserId" />
        <input type="hidden" asp-for="@Model[i].UserName" />
        <input asp-for="@Model[i].IsSelected" class="form-check-input" />
        <label multiple="multiple" class="form-check-label" asp-for="@Model[i].IsSelected">
            @Model[i].UserName
        </label>
    </div>}
            <input type="submit" value="Lägg till Student" class="btn btn-primary" />


        </form>
    </div>
</div>
<br>
<a asp-action="Index" class="btn btn-primary">Avbryt</a>